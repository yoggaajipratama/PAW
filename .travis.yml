sudo: required
language: php
os: trusty
php:
  - 7.1
  
#branche yg digunakan
branches:
  only:
    - master #-> QA/Staging server
    - master #-> DevServer

# install Environment di OS
before_script:
  # - curl -LO https://storage.googleapis.com/kubernet-release/release/$(curl -s https://storage.googleapis.com/kubernet-release/stable.txt) /bin/linux/amd64/kubectl
  # - chmod +x ./kubectl
  # - sudo mv ./kubectl /usr/localbin/kubenctl
  - cp .env.travis .env
  - composer install --no-interaction
  - php artisan key:genarate

# Main Exception (Build)
script:
  - ./vendor/bin/phpunit

  after_success: